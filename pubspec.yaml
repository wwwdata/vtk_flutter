name: vtk_flutter
description: Cross-platform Flutter bindings and texture integration for focused VTK volume rendering.
version: 0.1.0-dev.1
publish_to: none
homepage: https://github.com/wwwdata/vtk_flutter
repository: https://github.com/wwwdata/vtk_flutter
issue_tracker: https://github.com/wwwdata/vtk_flutter/issues
topics:
  - vtk
  - visualization
  - volume-rendering
  - ffi

# Keeps repository tooling independent from the not-yet-published native
# release. Consumer workspaces do not inherit workspace-root user defines.
hooks:
  user_defines:
    vtk_flutter:
      skip_native_artifact: true

environment:
  sdk: ^3.12.2
  flutter: '>=3.44.0'

dependencies:
  archive: ^4.0.7
  code_assets: ^1.0.0
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  web: ^1.0.0
  plugin_platform_interface: ^2.0.2
  ffi: ^2.2.0
  hooks: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.1.1
  test: ^1.28.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - assets/vtk_locator.js

  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: ninja.bieker.vtk_flutter
        pluginClass: VtkFlutterPlugin
        ffiPlugin: true
      ios:
        pluginClass: VtkFlutterPlugin
        ffiPlugin: true
      macos:
        pluginClass: VtkFlutterPlugin
        ffiPlugin: true
      windows:
        pluginClass: VtkFlutterPluginCApi
        ffiPlugin: true
      web:
        pluginClass: VtkFlutterWeb
        fileName: vtk_flutter_web.dart

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
